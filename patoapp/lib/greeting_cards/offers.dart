
import 'package:flutter/material.dart';

class OfferCards extends StatelessWidget {
  const OfferCards({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("OfferCards"),
    );
  }
}
