import 'package:flutter/material.dart';

class GoodMorningCards extends StatelessWidget {
  const GoodMorningCards({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("GoodMorningCards"),
    );
  }
}
